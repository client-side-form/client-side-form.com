---
layout: page.njk
title: "Normalizing Nested Field Error Paths"
description: "One canonical path notation, generated by the same function that parses it, so schema segment arrays and server JSON Pointers both resolve to the name the input was actually rendered with."
slug: normalizing-nested-field-error-paths
type: howto
breadcrumb: "Normalizing Nested Field Error Paths"
datePublished: "2026-08-05"
dateModified: "2026-08-05"
eleventyNavigation:
  key: "Normalizing Nested Field Error Paths"
  parent: "Error State Mapping Patterns"
  order: 2
---

<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@graph": [
    {
      "@type": "Article",
      "headline": "Normalizing Nested Field Error Paths",
      "description": "One canonical path notation, generated by the same function that parses it, so schema segment arrays and server JSON Pointers both resolve to the name the input was actually rendered with.",
      "datePublished": "2026-08-05",
      "dateModified": "2026-08-05",
      "author": {
        "@type": "Organization",
        "name": "client-side-form.com"
      }
    },
    {
      "@type": "BreadcrumbList",
      "itemListElement": [
        {
          "@type": "ListItem",
          "position": 1,
          "name": "Home",
          "item": "https://www.client-side-form.com/"
        },
        {
          "@type": "ListItem",
          "position": 2,
          "name": "Form State Fundamentals & Architecture",
          "item": "https://www.client-side-form.com/form-state-fundamentals-architecture/"
        },
        {
          "@type": "ListItem",
          "position": 3,
          "name": "Error State Mapping Patterns",
          "item": "https://www.client-side-form.com/form-state-fundamentals-architecture/error-state-mapping-patterns/"
        },
        {
          "@type": "ListItem",
          "position": 4,
          "name": "Normalizing Nested Field Error Paths",
          "item": "https://www.client-side-form.com/form-state-fundamentals-architecture/error-state-mapping-patterns/normalizing-nested-field-error-paths/"
        }
      ]
    },
    {
      "@type": "HowTo",
      "name": "Normalise nested field error paths to one notation",
      "step": [
        {
          "@type": "HowToStep",
          "position": 1,
          "name": "Choose a notation the DOM can carry"
        },
        {
          "@type": "HowToStep",
          "position": 2,
          "name": "Generate input names from the same function that parses them"
        },
        {
          "@type": "HowToStep",
          "position": 3,
          "name": "Convert each producer at the boundary"
        },
        {
          "@type": "HowToStep",
          "position": 4,
          "name": "Keep indices numeric through the conversion"
        },
        {
          "@type": "HowToStep",
          "position": 5,
          "name": "Decode pointer escapes in the correct order"
        },
        {
          "@type": "HowToStep",
          "position": 6,
          "name": "Promote unrecognised paths to the form-level summary"
        }
      ]
    },
    {
      "@type": "FAQPage",
      "mainEntity": [
        {
          "@type": "Question",
          "name": "Why dots and brackets rather than a segment array everywhere?",
          "acceptedAnswer": {
            "@type": "Answer",
            "text": "Because the DOM can only carry a string. The name attribute, the id, the summary link's href and the focus routine's selector all need one, so the notation that survives being written into markup is the one worth making canonical. Segment arrays are better inside code, which is why the pair of conversion functions exists — use arrays when you need to walk a value, strings everywhere the DOM is involved."
          }
        },
        {
          "@type": "Question",
          "name": "How do I handle a field whose key contains a dot?",
          "acceptedAnswer": {
            "@type": "Answer",
            "text": "Forbid it if you can — a key like user.name is ambiguous in every dotted notation and every consumer has to agree on the escaping. If it cannot be avoided, escape consistently in both directions and add a round-trip test for it, because a half-implemented escape scheme corrupts paths in a way that is very hard to trace back from a missing message."
          }
        },
        {
          "@type": "Question",
          "name": "What happens when rows are reordered between render and response?",
          "acceptedAnswer": {
            "@type": "Answer",
            "text": "The index in the path refers to a position in the payload that was submitted, not to whatever now occupies that position. Capture the row ids in submit order, and resolve incoming indices through that capture. Without it, deleting a row while a request is in flight moves an error onto an unrelated row, which is worse than not rendering it — the reader edits something that was never wrong."
          }
        }
      ]
    }
  ]
}
</script>

# Normalizing Nested Field Error Paths

The exact problem: a schema reports an issue at `["addresses", 1, "postcode"]`, a server reports the same failure at `/addresses/1/post_code`, and the input was rendered with `name="addresses[1].postcode"`. Three notations for one field, and the message renders under none of them.

## Context and Prerequisites

This is the path half of [error state mapping patterns](https://www.client-side-form.com/form-state-fundamentals-architecture/error-state-mapping-patterns/), which covers the shape an error lands in. The rule is that exactly one notation is canonical — the one the form generated when it rendered — and every producer is translated into it at the boundary.

## Core Pattern: One Canonical Notation

```typescript
/** Canonical form: dots for properties, brackets for indices. */
export type Path = string;   // "addresses[1].postcode"

/** From a segment array — what schema libraries emit. */
export function fromSegments(segments: readonly (string | number)[]): Path {
  return segments.reduce<string>((acc, seg) => {
    if (typeof seg === 'number') return `${acc}[${seg}]`;
    return acc ? `${acc}.${seg}` : String(seg);
  }, '');
}

/** From a JSON Pointer — what most APIs emit. */
export function fromPointer(pointer: string): Path {
  const segs = pointer.replace(/^\//, '').split('/')
    // Decode ~1 before ~0, or a legitimate "~1" in a key is corrupted.
    .map((s) => s.replace(/~1/g, '/').replace(/~0/g, '~'))
    .map((s) => (/^\d+$/.test(s) ? Number(s) : s));
  return fromSegments(segs);
}

/** Back to segments — needed to read or write a value at that path. */
export function toSegments(path: Path): (string | number)[] {
  return path.split('.').flatMap((part) => {
    const [head, ...idx] = part.split('[');
    return [head, ...idx.map((i) => Number(i.replace(']', '')))];
  }).filter((s) => s !== '');
}
```

The generator is the other half. The form must *emit* the canonical notation when it renders, because a translation that produces a correct string nothing was rendered with is still a message that does not appear:

```typescript
// The repeated fieldset knows its own path prefix and its index, so the name
// it renders and the name the resolver looks for are produced by one function.
const fieldName = (prefix: Path, index: number, key: string) =>
  fromSegments([...toSegments(prefix), index, key]);
```

<svg viewBox="0 8 690 214" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="Three producers of a field path — a schema segment array, a JSON Pointer and the rendered input name — converging on one canonical dotted-and-bracketed notation used by the error map, the summary and the focus routine." style="max-width:100%;height:auto;display:block;margin:1.5rem 0;">
  <title>Three notations in, one notation out</title>
  <desc>A schema library emits an array of segments mixing strings and numbers. A server emits a JSON Pointer string with slash separators and escape sequences. The rendered input carries a name attribute in the form's own notation. All three are converted into one canonical path using dots for properties and brackets for indices, and that single notation is what the error map is keyed by, what the summary links resolve against, and what the first-invalid focus routine matches on.</desc>
  <rect x="0" y="8" width="690" height="214" fill="#f9f5fb"/>
  <rect x="14" y="26" width="212" height="44" rx="7" fill="#ede5f2" stroke="#cbb8d9" stroke-width="1.5"/>
  <text x="28" y="46" font-size="10" font-weight="700" fill="#1e1a24" font-family="inherit">schema issue.path</text>
  <text x="28" y="62" font-size="9.5" fill="#6b5f75" font-family="inherit">["addresses", 1, "postcode"]</text>
  <rect x="14" y="86" width="212" height="44" rx="7" fill="#ede5f2" stroke="#cbb8d9" stroke-width="1.5"/>
  <text x="28" y="106" font-size="10" font-weight="700" fill="#1e1a24" font-family="inherit">server pointer</text>
  <text x="28" y="122" font-size="9.5" fill="#6b5f75" font-family="inherit">/addresses/1/post_code</text>
  <rect x="14" y="146" width="212" height="44" rx="7" fill="#ede5f2" stroke="#cbb8d9" stroke-width="1.5"/>
  <text x="28" y="166" font-size="10" font-weight="700" fill="#1e1a24" font-family="inherit">rendered input name</text>
  <text x="28" y="182" font-size="9.5" fill="#6b5f75" font-family="inherit">addresses[1].postcode</text>
  <path d="M226,48 H262 V108" fill="none" stroke="#7b4f8a" stroke-width="1.4"/>
  <path d="M226,108 H288" stroke="#7b4f8a" stroke-width="1.4"/>
  <path d="M226,168 H262 V108" fill="none" stroke="#7b4f8a" stroke-width="1.4"/>
  <rect x="288" y="82" width="180" height="52" rx="8" fill="#e2d6ec" stroke="#7b4f8a" stroke-width="2"/>
  <text x="378" y="106" text-anchor="middle" font-size="10.5" font-weight="700" fill="#1e1a24" font-family="inherit">canonical path</text>
  <text x="378" y="124" text-anchor="middle" font-size="9.5" fill="#1e1a24" font-family="inherit">addresses[1].postcode</text>
  <path d="M468,108 H504 V44" fill="none" stroke="#7b4f8a" stroke-width="1.4"/>
  <path d="M468,108 H530" stroke="#7b4f8a" stroke-width="1.4"/>
  <path d="M468,108 H504 V172" fill="none" stroke="#7b4f8a" stroke-width="1.4"/>
  <rect x="530" y="22" width="146" height="44" rx="7" fill="#ede5f2" stroke="#2d6342" stroke-width="1.5"/>
  <text x="544" y="42" font-size="10" fill="#2d6342" font-family="inherit">the error map key</text>
  <text x="544" y="58" font-size="9.5" fill="#6b5f75" font-family="inherit">one entry per field</text>
  <rect x="530" y="86" width="146" height="44" rx="7" fill="#ede5f2" stroke="#2d6342" stroke-width="1.5"/>
  <text x="544" y="106" font-size="10" fill="#2d6342" font-family="inherit">summary links</text>
  <text x="544" y="122" font-size="9.5" fill="#6b5f75" font-family="inherit">resolve to an id</text>
  <rect x="530" y="150" width="146" height="44" rx="7" fill="#ede5f2" stroke="#2d6342" stroke-width="1.5"/>
  <text x="544" y="170" font-size="10" fill="#2d6342" font-family="inherit">first-invalid focus</text>
  <text x="544" y="186" font-size="9.5" fill="#6b5f75" font-family="inherit">matches by name</text>
</svg>

<svg viewBox="0 8 690 240" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="A plain property is joined with a dot, unless it is the first segment, in which case it stands alone. An array index is written in brackets and attached directly to the preceding segment with no dot. A property following an index is joined with a dot as usual. And the root, an empty segment list, produces an empty string, which is the canonical way to say the whole object rather than a missing path." style="max-width:100%;height:auto;display:block;margin:1.5rem 0;">
  <title>The four segment kinds, and how each is written</title>
  <desc>A plain property is joined with a dot, unless it is the first segment, in which case it stands alone. An array index is written in brackets and attached directly to the preceding segment with no dot. A property following an index is joined with a dot as usual. And the root, an empty segment list, produces an empty string, which is the canonical way to say the whole object rather than a missing path.</desc>
  <rect x="0" y="8" width="690" height="240" fill="#f9f5fb"/>
  <rect x="10" y="16" width="670" height="166" rx="8" fill="none" stroke="#cbb8d9" stroke-width="1.5"/>
  <rect x="10" y="16" width="670" height="30" rx="8" fill="#e2d6ec"/>
  <rect x="10" y="36" width="670" height="10" fill="#e2d6ec"/>
  <text x="24" y="36" font-size="10.5" font-weight="700" fill="#1e1a24" font-family="inherit">Segment kind</text>
  <text x="220" y="36" font-size="10.5" font-weight="700" fill="#1e1a24" font-family="inherit">Written as</text>
  <text x="420" y="36" font-size="10.5" font-weight="700" fill="#1e1a24" font-family="inherit">Example</text>
  <text x="24" y="66" font-size="10" fill="#1e1a24" font-family="inherit">the first property</text>
  <text x="220" y="66" font-size="10" fill="#6b5f75" font-family="inherit">bare</text>
  <text x="420" y="66" font-size="10" fill="#6b5f75" font-family="inherit">addresses</text>
  <line x1="10" y1="80" x2="680" y2="80" stroke="#cbb8d9" stroke-width="1"/>
  <text x="24" y="100" font-size="10" fill="#1e1a24" font-family="inherit">a later property</text>
  <text x="220" y="100" font-size="10" fill="#6b5f75" font-family="inherit">preceded by a dot</text>
  <text x="420" y="100" font-size="10" fill="#6b5f75" font-family="inherit">.postcode</text>
  <line x1="10" y1="114" x2="680" y2="114" stroke="#cbb8d9" stroke-width="1"/>
  <text x="24" y="134" font-size="10" fill="#1e1a24" font-family="inherit">an array index</text>
  <text x="220" y="134" font-size="10" fill="#6b5f75" font-family="inherit">brackets, no dot</text>
  <text x="420" y="134" font-size="10" fill="#6b5f75" font-family="inherit">[1]</text>
  <line x1="10" y1="148" x2="680" y2="148" stroke="#cbb8d9" stroke-width="1"/>
  <text x="24" y="168" font-size="10" fill="#1e1a24" font-family="inherit">the root</text>
  <text x="220" y="168" font-size="10" fill="#6b5f75" font-family="inherit">an empty string</text>
  <text x="420" y="168" font-size="10" fill="#6b5f75" font-family="inherit">""</text>
  <text x="14" y="226" font-size="10" fill="#6b5f75" font-family="inherit">The empty string is deliberate: it is a real value meaning "the whole object", distinct from a path that failed to resolve.</text>
</svg>

## Step-by-Step Walkthrough

1. **Pick the notation the DOM can carry.** Dots and brackets survive being a `name` attribute and an `id`; a segment array does not.

2. **Generate names from the same function that parses them.** If `fieldName` and `toSegments` are inverses, a rename cannot desynchronise them.

3. **Translate at the boundary, once.** One adapter per producer, each tested against captured payloads.

4. **Keep indices numeric through the conversion.** `"1"` and `1` behave differently the moment you use the segments to read a value.

5. **Decode pointer escapes in the right order.** `~1` before `~0`, or a key legitimately containing `~1` is corrupted.

6. **Fall back visibly.** An unrecognised path goes to the form-level summary, never to a log alone.

## Failure Modes and Edge Cases

### 1. Positions versus identities

`addresses[1]` is a position. Delete row 0 and the same string now names a different address. Where rows can be reordered or removed, key the render by a stable id and translate positions through the ordering captured at submit.

### 2. Keys containing dots

A field literally named `user.name` is ambiguous in dotted notation. Either forbid it, or escape it consistently in both directions — and test the round trip, because half-implemented escaping is worse than none.

### 3. Root-level issues

An empty segment array or a pointer of `""` means the whole object. That is a form-level error and should be routed as one, not treated as a translation failure.

### 4. Sparse arrays

An issue at index 3 of an array rendered with two rows resolves to nothing. Promote it rather than dropping it — it usually means the client and server disagree about the payload, which is worth surfacing.

### 5. Two producers, one field

A schema issue and a server error for the same canonical path both belong in the map. Keep both and render by precedence, as [error state mapping patterns](https://www.client-side-form.com/form-state-fundamentals-architecture/error-state-mapping-patterns/) describes.

<svg viewBox="0 8 690 168" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="The form generates a name from a prefix, an index and a key. That string is what the input carries and what a summary link resolves against. A producer later reports a path in its own notation, which the adapter converts into the same string. Parsing that string back into segments must return exactly what generated it — otherwise reading or writing the value at that path lands somewhere else, and the message renders on the wrong field." style="max-width:100%;height:auto;display:block;margin:1.5rem 0;">
  <title>The round trip that has to hold</title>
  <desc>The form generates a name from a prefix, an index and a key. That string is what the input carries and what a summary link resolves against. A producer later reports a path in its own notation, which the adapter converts into the same string. Parsing that string back into segments must return exactly what generated it — otherwise reading or writing the value at that path lands somewhere else, and the message renders on the wrong field.</desc>
  <rect x="0" y="8" width="690" height="168" fill="#f9f5fb"/>
  <text x="14" y="30" font-size="12" font-weight="700" fill="#1e1a24" font-family="inherit">The round trip that has to hold</text>
  <rect x="14" y="42" width="149" height="76" rx="8" fill="#ede5f2" stroke="#7b4f8a" stroke-width="1.5"/>
  <text x="88" y="66" text-anchor="middle" font-size="10.5" font-weight="700" fill="#7b4f8a" font-family="inherit">generate</text>
  <text x="88" y="86" text-anchor="middle" font-size="9.5" fill="#6b5f75" font-family="inherit">prefix + index + key</text>
  <text x="88" y="102" text-anchor="middle" font-size="9.5" fill="#6b5f75" font-family="inherit">becomes the input name</text>
  <path d="M163,80 H185" stroke="#7b4f8a" stroke-width="1.4"/>
  <rect x="185" y="42" width="149" height="76" rx="8" fill="#ede5f2" stroke="#6b5f75" stroke-width="1.5"/>
  <text x="259" y="66" text-anchor="middle" font-size="10.5" font-weight="700" fill="#6b5f75" font-family="inherit">report</text>
  <text x="259" y="86" text-anchor="middle" font-size="9.5" fill="#6b5f75" font-family="inherit">a producer emits its</text>
  <text x="259" y="102" text-anchor="middle" font-size="9.5" fill="#6b5f75" font-family="inherit">own notation</text>
  <path d="M334,80 H356" stroke="#7b4f8a" stroke-width="1.4"/>
  <rect x="356" y="42" width="149" height="76" rx="8" fill="#ede5f2" stroke="#7b4f8a" stroke-width="1.5"/>
  <text x="430" y="66" text-anchor="middle" font-size="10.5" font-weight="700" fill="#7b4f8a" font-family="inherit">convert</text>
  <text x="430" y="86" text-anchor="middle" font-size="9.5" fill="#6b5f75" font-family="inherit">the adapter produces</text>
  <text x="430" y="102" text-anchor="middle" font-size="9.5" fill="#6b5f75" font-family="inherit">the same string</text>
  <path d="M505,80 H527" stroke="#7b4f8a" stroke-width="1.4"/>
  <rect x="527" y="42" width="149" height="76" rx="8" fill="#ede5f2" stroke="#2d6342" stroke-width="1.5"/>
  <text x="601" y="66" text-anchor="middle" font-size="10.5" font-weight="700" fill="#2d6342" font-family="inherit">parse back</text>
  <text x="601" y="86" text-anchor="middle" font-size="9.5" fill="#6b5f75" font-family="inherit">segments must equal</text>
  <text x="601" y="102" text-anchor="middle" font-size="9.5" fill="#6b5f75" font-family="inherit">what generated it</text>
  <text x="14" y="142" font-size="10" fill="#6b5f75" font-family="inherit">Assert that round trip over every path shape the form can produce; it is a three-line test and it catches every notation bug.</text>
</svg>

## Verification Checklist

- [ ] `fromSegments` and `toSegments` round-trip every path the form can generate
- [ ] A JSON Pointer with escapes converts correctly
- [ ] Indices stay numeric through the conversion
- [ ] The rendered `name` and the resolver's key come from one function
- [ ] A root-level issue is routed to the form-level summary
- [ ] An index beyond the rendered rows is promoted, not dropped
- [ ] Reordering rows does not move an error onto the wrong one

## Common Pitfalls

- **Two notations in the codebase.** The moment a second one appears, some producers resolve and others silently do not, and the difference only shows up as a missing message on one particular field shape.
- **Hand-writing input names.** A generated name and a hand-written resolver key drift the first time a schema field is renamed, and nothing fails — the message simply stops appearing.
- **Treating indices as identities.** A path names a position in the payload that was submitted. Delete a row while a request is in flight and the same path now points at a different row.
- **Decoding pointer escapes in the wrong order.** Replacing `~0` before `~1` corrupts any key that legitimately contains a tilde followed by a one, which is rare and extremely hard to trace.
- **Dropping unresolvable paths.** A submit that fails with nothing on screen is the worst outcome in this whole area. Route anything that does not resolve to the form-level summary.

---

**Related**

- [Error State Mapping Patterns](https://www.client-side-form.com/form-state-fundamentals-architecture/error-state-mapping-patterns/) — the shape the path is a key into
- [Mapping 422 Responses to Field Errors](https://www.client-side-form.com/validation-logic-schema-integration/server-error-reconciliation/mapping-422-responses-to-field-errors/) — the server-side producer
- [How to Validate Dependent Fields with Zod](https://www.client-side-form.com/validation-logic-schema-integration/integrating-zod-for-schema-validation/how-to-validate-dependent-fields-with-zod/) — where issue paths are set

← [Error State Mapping Patterns](https://www.client-side-form.com/form-state-fundamentals-architecture/error-state-mapping-patterns/)

## Frequently Asked Questions

<details>
<summary><strong>Why dots and brackets rather than a segment array everywhere?</strong></summary>

Because the DOM can only carry a string. The name attribute, the id, the summary link's href and the focus routine's selector all need one, so the notation that survives being written into markup is the one worth making canonical. Segment arrays are better inside code, which is why the pair of conversion functions exists — use arrays when you need to walk a value, strings everywhere the DOM is involved.

</details>

<details>
<summary><strong>How do I handle a field whose key contains a dot?</strong></summary>

Forbid it if you can — a key like user.name is ambiguous in every dotted notation and every consumer has to agree on the escaping. If it cannot be avoided, escape consistently in both directions and add a round-trip test for it, because a half-implemented escape scheme corrupts paths in a way that is very hard to trace back from a missing message.

</details>

<details>
<summary><strong>What happens when rows are reordered between render and response?</strong></summary>

The index in the path refers to a position in the payload that was submitted, not to whatever now occupies that position. Capture the row ids in submit order, and resolve incoming indices through that capture. Without it, deleting a row while a request is in flight moves an error onto an unrelated row, which is worse than not rendering it — the reader edits something that was never wrong.

</details>

